#!/bin/bash

# Скрипт для быстрого развертывания обновленной админ-панели
# с поддержкой VK Video iframe

echo "🚀 Развертывание админ-панели с поддержкой VK Video iframe..."
echo ""

# Переход в директорию админ-панели
cd "$(dirname "$0")/admin-panel" || exit 1

# Проверка наличия node_modules
if [ ! -d "node_modules" ]; then
    echo "📦 Установка зависимостей..."
    npm install
fi

# Сборка проекта
echo "🔨 Сборка проекта..."
npm run build

if [ $? -eq 0 ]; then
    echo ""
    echo "✅ Сборка завершена успешно!"
    echo ""
    echo "📁 Файлы для развертывания находятся в: admin-panel/dist/"
    echo ""
    echo "Следующие шаги:"
    echo "1. Скопируйте содержимое dist/ на сервер"
    echo "2. Или используйте скрипт deploy-admin.sh для автоматического развертывания"
    echo ""
    echo "🎯 Тестирование:"
    echo "- Откройте админ-панель в браузере"
    echo "- Перейдите в редактор марафона"
    echo "- Нажмите кнопку '🎥 Видео'"
    echo "- Вставьте iframe код от ВКонтакте"
    echo "- Убедитесь, что видео отображается корректно"
else
    echo ""
    echo "❌ Ошибка при сборке!"
    exit 1
fi
