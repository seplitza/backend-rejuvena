# Обновления для Rejuvena Admin - 1 марта 2026

## ✅ Выполненные изменения

### 1. Исправлен независимый скролл меню и контента
**Проблема:** При прокрутке меню пропадал контент страницы и наоборот
**Решение:** 
- Сайдбар теперь фиксированный слева с независимым скроллом
- Контент справа также скроллится независимо
- Оба элемента находятся в viewport без взаимного влияния

**Файл:** `admin-panel/src/components/Layout.tsx`

### 2. Добавлена система управления токеном Wildberries
**Проблема:** Токен WB можно было задать только через .env на сервере
**Решение:**
- Создана модель Settings для хранения конфигурации в БД
- Добавлено поле ввода токена на странице Wildberries
- API endpoints: GET/POST `/api/admin/wildberries/token`
- Токен загружается из БД при старте сервиса
- Можно обновить токен через UI без перезапуска сервера

**Файлы:**
- `src/models/Settings.model.ts` - модель для настроек
- `src/routes/admin/wildberries-admin.routes.ts` - endpoints токена  
- `src/services/wildberries.service.ts` - загрузка токена из БД
- `admin-panel/src/pages/WildberriesIntegration.tsx` - UI для токена

### 3. Товары пусты - это нормально
На скриншоте видно "Найдено товаров: 0 из 0" - это означает что в БД еще нет товаров.

**Что делать:**
1. Нажмите "+ Добавить товар" в разделе Товары
2. Или используйте **Импорт данных** (см. ниже)

### 4. Система импорта данных уже создана ✅
**Важно:** Страница импорта УЖЕ добавлена в меню и роуты!

**Расположение:** Админ-панель → Магазин → 📥 Импорт данных

**Возможности:**
- Загрузка CSV и JSON файлов
- Импорт заказов, платежей, пользователей
- Предпросмотр перед импортом
- 3 режима: insert/upsert/replace

**Если не видите:**
1. Очистите кэш браузера (Ctrl+Shift+R / Cmd+Shift+R)
2. Проверьте что админ-панель пересобрана на сервере (см. ниже)

## 🚀 Деплой на production

### Автодеплой через GitHub Actions
GitHub Actions автоматически задеплоит backend после push в main.

### Ручной деплой админ-панели (если автодеплой не сработал)

```bash
# SSH на сервер
ssh root@37.252.20.170

# Перейти в проект
cd /var/www/rejuvena-backend

# Обновить код
git pull origin main

# Установить зависимости (если есть новые)
npm install

# Пересобрать backend
npm run build

# Перезапустить backend
pm2 restart rejuvena-backend

# Пересобрать админ-панель
cd admin-panel
npm install
npm run build

# Админка теперь доступна по https://api.seplitza.ru/admin/
```

### Проверка после деплоя

1. **Импорт данных:**
   - https://api.seplitza.ru/admin/data-import
   - Должна открыться страница с drag-and-drop зоной

2. **Wildberries интеграция:**
   - https://api.seplitza.ru/admin/wildberries
   - Должно быть поле "Настройка API токена"
   - Вставьте токен который вы давали ранее
   - Нажмите "Сохранить токен"
   - Нажмите "Проверить подключение"
   - Должно показать "✅ Подключено"

3. **Скролл меню:**
   - Прокрутите меню вниз до "Импорт данных"
   - Убедитесь что правая часть экрана НЕ прокручивается вместе с меню

## 📋 Инструкции по использованию

### Как добавить токен Wildberries:
1. Откройте админ-панель → Wildberries
2. В секции "Настройка API токена" вставьте ваш токен
3. Нажмите "Сохранить токен"
4. Токен сохранится в БД и будет использоваться для всех запросов
5. При необходимости можно обновить токен в любой момент

### Как использовать импорт данных:
1. Откройте админ-панель → Импорт данных
2. Перетащите CSV или JSON файл в зону загрузки
3. Нажмите "Предпросмотр" - увидите первые 10 записей
4. Выберите режим импорта (рекомендуется "upsert")
5. Нажмите "Импортировать"
6. Результаты отобразятся ниже

Примеры файлов: `/test-data/sample-orders.csv`, `/test-data/sample-payments.json`

Подробная документация: `DATA_IMPORT_SYSTEM.md`

## 🐛 Возможные проблемы

### Проблема: Не вижу страницу "Импорт данных"
**Решение:**
1. Очистить кэш браузера (Ctrl+Shift+R)
2. Проверить что админ-панель пересобрана: `cd admin-panel && npm run build`
3. Если не помогло - пересобрать на production (см. инструкцию выше)

### Проблема: Wildberries показывает "WB_API_TOKEN not configured"
**Решение:**
1. Прокрутите страницу вверх
2. Найдите секцию "Настройка API токена"  
3. Вставьте токен и нажмите "Сохранить"
4. Нажмите "Проверить подключение" еще раз

### Проблема: Товары пусты
**Это нормально!** В БД нет товаров.

**Решения:**
1. Добавьте вручную через "+ Добавить товар"
2. Используйте импорт данных (если есть CSV с товарами)

## 📞 Поддержка

Все изменения задеплоены в коммитах:
- `011f9e8` - Документация импорта
- `f07df0f` - WB токен + скролл

Если что-то не работает - проверьте:
1. Админ-панель пересобрана?
2. Backend перезапущен?
3. Кэш браузера очищен?

---

**Статус:** Все готово к использованию ✅  
**Дата:** 1 марта 2026  
**Коммит:** f07df0f
